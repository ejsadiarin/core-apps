---
services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - coregateway-api

  coregateway-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=8080
      - ENV=development
      - ALLOWED_ORIGINS=http://localhost:3000
      - ADMIN_EMAIL=${ADMIN_EMAIL:-}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-}

  corefinance-api:
    build:
      context: ./services/corefinance
      dockerfile: Dockerfile
    ports:
      - "6969:6969"
    environment:
      - ENV=development
      - DATABASE_URL=${DATABASE_URL}
      - DB_SCHEMA=corefinance
      - PORT=6969
